let num = 5 // here num is a variable that stores number 5.


//In JavaScript, we use the var or let keywords to declare variables. For example,
 var num;
 let num;

 // declare a vaiable num
let num;

// assignment 5 to variable num
num = 5;

// you can declare and assign a variable in one step

let num = 5;
let a = 5;

// we can also declare multiple variable at same time
  let num1 = 4, num2 = 3, num3 = 2;

  //If you use a variable without initializing it, it will have an undefined value

  //declare variable num
  let num;
  //print variable num
  console.log(num);
  //output will undefined.
   

  //   CHANGE THE VALUE OF VARIABLE INSIDE A CODE.
   let score = 1
   console.log(score);
   score = 4;
   console.log(score);
   // output wil update to 4 from 1

   // variables can be declare in the following ways

   let _message = 1;// using underscore
   let $message = 1;// using dollar symbol
   let message = 1;// using normally.
   let 1message = 1 // this cannot be assinged coz we are adding a number bofore a vaiable.


   // js is case sensitive
   let age = 1;
   let Age = 3;

   console.log(age);// output = 1
   console.log(Age);//output = 3


   // keyword : keywords are special words that we cannot place in variable.
     let var = 2;
     let if = 2;
     let else = 2;


     //JavaScript Constants

     //A constant is a type of variable whose value cannot be changed.

     // In JavaScript, we use the const keyword to create constants. For example,

     const num = 2;
     const num = 1;
     // assign 5 to const num
     const num = 5;
     // update 10 to const num
     num = 10;
    
     console.log(num); // output will be undefined coz const variable cannot update.

     